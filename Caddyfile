{
	admin off
	persist_config off
	auto_https off
	log {
		format json
	}
	servers {
		trusted_proxies static private_ranges
	}
}

:{$PORT} {
	log {
		format json
	}
	rewrite /health /*
	root * {$ANGULAR_OUTPUT_PATH}/browser
	encode gzip
	file_server
	try_files {path} /index.html
}
